You are analyzing the following ML inferences and network data:

{data}

Based on this data, select the most appropriate action(s) from the available decisions below.

═══════════════════════════════════════════════════════════════
AVAILABLE DECISIONS:
{decisions}
═══════════════════════════════════════════════════════════════

CRITICAL INSTRUCTIONS:

1. EXACT ID MATCHING (NON-NEGOTIABLE):
   - Copy decision IDs EXACTLY as shown above, character-for-character
   - Do NOT modify, shorten, or remove any part of the ID
   - If the ID contains <<parameter>>, keep it: use "ban <<ip_src>>" not "ban"

2. TEMPLATE PARAMETERS:
   - For decisions with <<param>> (e.g., "ban <<ip_src>>"):
     * Use the EXACT ID including <<>>: "ban <<ip_src>>"
     * Extract the actual value from the ML data
     * Provide it in "args": {{"ip_src": "192.168.1.100"}}

   - For decisions without parameters (e.g., "alert_operator"):
     * Use the EXACT ID: "alert_operator"
     * Provide empty args: {{}}

3. REQUIRED OUTPUT FORMAT:
{{
  "decisions": [
    {{"id": "exact_decision_id_from_list", "args": {{}}}},
    {{"id": "ban <<ip_src>>", "args": {{"ip_src": "actual_value"}}}}
  ],
  "reasoning": "Clear explanation citing specific ML metrics, anomaly scores, or predictions. Reference actual data points.",
  "alternatives": [
    {{"id": "other_decision_id", "args": {{}}}}
  ]
}}

4. REASONING REQUIREMENTS:
   - Must reference specific data points (e.g., "IP 192.168.1.100 shows 15/20 anomalous windows")
   - Explain WHY each decision was chosen based on ML confidence, severity, or trends
   - Keep concise but informative (2-3 sentences)

5. SELECTION GUIDELINES:
   - High anomaly scores (>0.8) or many anomalous windows → consider "ban <<ip_src>>" or "alert_operator"
   - Traffic forecast predicts congestion → "reduce_quality" or "increase_bandwidth"
   - Critical thresholds breached → "alert_operator"
   - No significant issues → "do_nothing"

Return ONLY the JSON object. Do not include any text before or after the JSON.
